# Leave empty in dev (Vite proxies /api -> backend:8787). Set for prod build.
VITE_API_BASE=

# LIFF app id from the LINE Developers console (LIFF tab of the LINE_LIFF_CHANNEL_ID channel).
# Customer LIFF page (liff.html) is opened with ?oa=<line_oa_account_id> to select the OA.
VITE_LIFF_ID=
